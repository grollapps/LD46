using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// Type A attributes: primary (inverse)
/// Smart (dumb)
/// Fertile (sterile)
/// Unstable (stable or non-random)
/// Fast (slow)
/// Valuable (not-valuable)
/// </summary>
public class StatA : MonoBehaviour
{

    public float smart;
    public float fertile;
    public float unstable;
    public float fast;
    public float valuable;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
